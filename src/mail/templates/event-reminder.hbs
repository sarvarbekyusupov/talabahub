<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Event Reminder</title>
    <style>
        body { font-family: Arial, sans-serif; line-height: 1.6; color: #333; }
        .container { max-width: 600px; margin: 0 auto; padding: 20px; }
        .header { background: #EC4899; color: white; padding: 20px; text-align: center; }
        .content { background: #f9f9f9; padding: 30px; }
        .reminder-box { background: #FEF3C7; padding: 20px; margin: 20px 0; border-radius: 5px; text-align: center; }
        .event-details { background: white; padding: 20px; margin: 20px 0; }
        .footer { text-align: center; padding: 20px; color: #666; font-size: 12px; }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>⏰ Event Tomorrow!</h1>
        </div>
        <div class="content">
            <h2>Hi {{name}},</h2>

            <div class="reminder-box">
                <h3 style="margin: 0;">Don't forget!</h3>
                <p style="font-size: 18px; margin: 10px 0;">Your event is tomorrow</p>
            </div>

            <div class="event-details">
                <h3>{{eventTitle}}</h3>
                <p><strong>📅 Date:</strong> {{eventDate}}</p>
                <p><strong>🕐 Time:</strong> {{eventTime}}</p>
                {{#if location}}
                <p><strong>📍 Location:</strong> {{location}}</p>
                {{/if}}
                {{#if meetingLink}}
                <p><strong>🔗 Join Link:</strong> <a href="{{meetingLink}}">{{meetingLink}}</a></p>
                {{/if}}
            </div>

            <p>See you there! 👋</p>
        </div>
        <div class="footer">
            <p>&copy; 2024 TalabaHub. All rights reserved.</p>
        </div>
    </div>
</body>
</html>
